# kaggle

## install
```  
  virtualenv --system-site-packages -p python3 env
  source env/bin/activate
  pip3 install -r keras tensorflow tensorflow-gpu opencv-python pandas tqdm sklearn

  ln -s ~/data ./ 
```
